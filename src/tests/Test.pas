program Prueba;
var
   a,b: integer;
const
     PI: real = 3.141592;
     SALUDO: string = 'HOLA MUNDO';
begin
  write();
  write();
  write();
  write();
  write();
  readln(a,b);
  if a>b then writeln(SALUDO);
  else writeln ('a es igual que b');
end.